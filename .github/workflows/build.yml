name: Build APK and Daemon

on:
  push:
    branches: [ '**' ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK and NDK
        uses: android-actions/setup-android@v4
        with:
          packages: "platform-tools build-tools;34.0.0 ndk;29.0.13113456"
          accept-android-sdk-licenses: true

      - name: Set NDK_DIR environment variable
        run: echo "NDK_DIR=$ANDROID_HOME/ndk/29.0.13113456" >> $GITHUB_ENV

      - name: Generate debug keystore
        run: |
          mkdir -p $HOME/.android
          keytool -genkeypair -v -keystore $HOME/.android/debug.keystore \
            -storepass android -alias androiddebugkey -keypass android \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Android Debug, O=Android, C=US" -noprompt

      - name: Inject debug signing for release
        working-directory: consumers/anland_v5/android_consumer
        run: |
          echo "android.injected.signing.store.file=$HOME/.android/debug.keystore" >> gradle.properties
          echo "android.injected.signing.store.password=android" >> gradle.properties
          echo "android.injected.signing.key.alias=androiddebugkey" >> gradle.properties
          echo "android.injected.signing.key.password=android" >> gradle.properties

      - name: Build APK
        working-directory: consumers/anland_v5/android_consumer
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease
          cp app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/anland-v5.apk

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: anland-v5
          path: consumers/anland_v5/android_consumer/app/build/outputs/apk/release/anland-v5.apk

      - name: Build APK (Landscape)
        working-directory: consumers/anland_v5/android_consumer
        run: |
          sed -i 's/android:screenOrientation="fullSensor"/android:screenOrientation="sensorLandscape"/g' app/src/main/AndroidManifest.xml
          ./gradlew clean assembleRelease
          cp app/build/outputs/apk/release/app-release.apk app/build/outputs/apk/release/anland-v5-landscape.apk

      - name: Upload APK (Landscape)
        uses: actions/upload-artifact@v7
        with:
          name: anland-v5-landscape
          path: consumers/anland_v5/android_consumer/app/build/outputs/apk/release/anland-v5-landscape.apk

      - name: Configure CMake for Android
        run: |
          cmake -S . -B build_daemon_android \
            -DCMAKE_TOOLCHAIN_FILE="$NDK_DIR/build/cmake/android.toolchain.cmake" \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-30 \
            -DCMAKE_BUILD_TYPE=Release

      - name: Build display_daemon
        run: cmake --build build_daemon_android --target display_daemon -j$(nproc)

      - name: Copy daemon to magisk_module directory
        run: |
          cp build_daemon_android/display_daemon magisk_module/
          chmod 755 magisk_module/display_daemon

      - name: Upload Magisk module
        uses: actions/upload-artifact@v7
        with:
          name: anland-daemon
          path: magisk_module/